<%= form_for([@post, @post.comments.build]) do |f| %>

    <%= f.rich_text_area :content,:size=>"50", placeholder: "Leave a comment ", class: 'form-control post_content',data: {controller: "mentions", target: "mentions.field"} %>
    <br>
    <%= f.submit class: 'btn btn-primary' %>
    
<%end%>