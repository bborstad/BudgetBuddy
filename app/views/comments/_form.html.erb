<%= form_for([@post, @post.comments.build]) do |f| %>

    <%= f.text_area :content,:size=>"50", placeholder: "Leave a comment ", class: 'form-control post_content's %>
    <br>
    <%= f.submit class: 'btn btn-primary' %>
    
<%end%>